import {LoginComponent} from "@/components/login/LoginComponent";

const LoginPage = () => {
    return (
        <div>
            <LoginComponent/>
        </div>
    );
};

export default LoginPage;